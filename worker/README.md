# Future backend

Reserved for versioned API handlers and scheduled source adapters. Not deployed in v0.1. Follow ../docs/architecture.md. Introduce a Worker entry point and bindings only in a separately tested deployment change; retain the existing public asset directory.
