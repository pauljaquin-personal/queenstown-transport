# Worker backend

The Worker now handles only `/api/*` routes while static assets remain on Cloudflare Workers Static Assets.

Current endpoint: privacy-first My Commute collection backed by an optional D1 binding named `COMMUTES`.

See `../docs/commutes.md` for schema, privacy constraints and setup.
